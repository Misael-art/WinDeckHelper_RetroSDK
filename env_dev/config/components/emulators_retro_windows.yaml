BizHawk:
  category: Emulators (Retro - Windows)
  description: Emulador multi-sistema focado em TAS (Tool-Assisted Speedruns)
  download_url: https://github.com/TASEmulators/BizHawk/releases
  install_method: none
  post_install_message: Acesse https://github.com/TASEmulators/BizHawk/releases para
    baixar o ZIP do BizHawk e extraia-o manualmente. Certifique-se de ter os pré-requisitos
    (.NET Desktop Runtime, VC++ Runtimes) instalados.
  verify_actions:
  - path: ${env:ProgramFiles}\BizHawk\EmuHawk.exe
    type: file_exists
Mednafen:
  category: Emulators (Retro - Windows)
  dependencies:
  - 7-Zip
  description: Emulador multi-sistema via linha de comando (NES, GB/A, PS1, PCE, etc.)
  download_url: https://mednafen.github.io/releases/files/mednafen-1.29.0-win64.zip
  extract_path: ${env:ProgramFiles}\Mednafen
  hash: HASH_PENDENTE_VERIFICACAO
  hash_algorithm: sha256
  install_method: archive
  verify_actions:
  - path: ${env:ProgramFiles}\Mednafen\mednafen.exe
    type: file_exists
Mesen:
  category: Emulators (Retro - Windows)
  dependencies:
  - 7-Zip
  description: Emulador de NES/Famicom, SNES/Super Famicom, Game Boy, PC Engine de
    alta precisão
  download_url: https://github.com/SourMesen/Mesen2/releases/latest/download/Mesen-Windows.zip
  extract_path: ${env:ProgramFiles}\Mesen
  hash: HASH_NEEDS_UPDATE
  hash_algorithm: sha256
  install_method: archive
  verify_actions:
  - path: ${env:ProgramFiles}\Mesen\Mesen.exe
    type: file_exists
VBA-M (VisualBoyAdvance-M):
  category: Emulators (Retro - Windows)
  dependencies:
  - 7-Zip
  description: Emulador de Game Boy / Color / Advance
  download_url: https://github.com/visualboyadvance-m/visualboyadvance-m/releases/latest/download/visualboyadvance-m-Win-x64.zip
  extract_path: ${env:ProgramFiles}\VBA-M
  hash: HASH_NEEDS_UPDATE
  hash_algorithm: sha256
  install_method: archive
  verify_actions:
  - path: ${env:ProgramFiles}\VBA-M\visualboyadvance-m.exe
    type: file_exists
mGBA:
  category: Emulators (Retro - Windows)
  dependencies:
  - 7-Zip
  description: Emulador de Game Boy Advance rápido e preciso
  download_url: https://github.com/mgba-emu/mgba/releases/latest/download/mGBA-0.11.1-win64.7z
  extract_path: ${env:ProgramFiles}\mGBA
  hash: HASH_NEEDS_UPDATE
  hash_algorithm: sha256
  install_method: archive
  verify_actions:
  - path: ${env:ProgramFiles}\mGBA\mGBA.exe
    type: file_exists
